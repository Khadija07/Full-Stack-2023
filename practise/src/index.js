import React from 'react'
import ReactDOM from 'react-dom/client'

import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(<App />)

// this is page re-rendering,not a recommended way
/* setInterval(() => { 
  refresh()
  counter += 1
}, 1000) */

//stateful component

